---
layout: multiple_layout
---
<body id="home">
	<!-- intro -->
	<div class="flexcontainer">
		<div class="col-10">
			<div class="padded center question">
				<h2>What is the most important factor for your visualization to show?</h2>
				<p>A key principle to data visualization is that the visuals should always heed the data;<br>
				data should never be unnaturally fitted into a visualization.</p>
				<div class="tagcontainer">
					<a class="tag" href="">Hierarchy</a> 
					<a href="#range" class="tag">Range</a>
						<div class="cd-popup" role="alert">
							<div class="cd-popup-container">
								<h2>Range</h2>
								<p>Is it correct that you have data where the distance distance between the two limits of each set is meaningful?</p><br>
								<p>Commonly used for</p><br>
								<div class="flexcontainer">
									<div class="col-4">
										<img class="iconlrg" src="images/temp_time.png"><br>
										<p>Time spans</p>
									</div>
									<div class="col-4">
										<img class="iconlrg" src="images/temp_health.png"><br>
										<p>Blood pressure</p>
									</div>
									<div class="col-4">
										<img class="iconlrg" src="images/temp_price.png"><br>
										<p>Price ranges</p>
									</div>
								</div>
								<a class="btn space" href="">No, let me select again</a>
								<a class="btn" href="range.html">Yes, continue</a>
							</div> <!-- cd-popup-container -->
						</div> <!-- cd-popup -->
					<a class="tag" href="">Cycles</a> 
					<a class="tag" href="">Flow</a> 
					<a class="tag" href="">Continuous intervals</a> 
					<a class="tag" href="">Aggregations</a> 
					<a class="tag" href="">Deviation from normal</a> 
					<a class="tag" href="">Proportions</a> 
					<a class="tag" href="">Pattern</a> 
					<a class="tag" href="">Location</a> 
					<a class="tag" href="">Change over time</a>
				</div>
			</div>
		</div>
	</div>

<script type="text/javascript">
	jQuery(document).ready(function($){
	//open popup
	$('.tag').on('click', function(event){
		event.preventDefault();
		$('.cd-popup').addClass('is-visible');
	});
	
	//close popup
	$('.cd-popup').on('click', function(event){
		if( $(event.target).is('.cd-popup-close') || $(event.target).is('.cd-popup') ) {
			event.preventDefault();
			$(this).removeClass('is-visible');
		}
	});
	//close popup when clicking the esc keyboard button
	$(document).keyup(function(event){
    	if(event.which=='27'){
    		$('.cd-popup').removeClass('is-visible');
	    }
    });
});
</script>
</body>
